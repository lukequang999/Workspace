cout << "Duyet cay theo thu tu truoc (pre-order):" << endl;
preOrderTraversal(tree.root);

cout << "Duyet cay theo thu tu giua (in-order):" << endl;
inOrderTraversal(tree.root);

cout << "Duyet cay theo thu tu sau (post-order):" << endl;
postOrderTraversal(tree.root);
